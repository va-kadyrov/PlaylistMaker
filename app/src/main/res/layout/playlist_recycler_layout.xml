<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="13dp">

        <ImageView
            android:id="@+id/playlist_item_img"
            android:layout_width="@dimen/playlist_item_img"
            android:layout_height="@dimen/playlist_item_img"
            android:scaleType="fitCenter"
            android:src="@drawable/album_cover_empty"/>

        <TextView
            android:id="@+id/playlist_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:textSize="12sp"
            android:textStyle="normal"
            android:textColor="@color/almostblack_white"
            android:maxLines="1"
            tools:text="name" />

        <TextView
            android:id="@+id/playlist_item_tracks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:textSize="12sp"
            android:textStyle="normal"
            android:textColor="@color/almostblack_white"
            android:maxLines="1"
            tools:text="Description " />
    </LinearLayout>

