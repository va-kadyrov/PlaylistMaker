<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="3dp"
    android:background="@null"
    card_view:cardElevation="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="13dp"
        android:paddingEnd="44dp">

        <ImageView
            android:id="@+id/playlist_item_img"
            android:layout_width="@dimen/playlist_item_img_small"
            android:layout_height="@dimen/playlist_item_img_small"
            android:scaleType="fitCenter"
            android:src="@drawable/album_cover_empty"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="8dp">

        <TextView
            android:id="@+id/playlist_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:layout_marginBottom="1dp"
            android:textColor="@color/black_white"
            android:ellipsize="end"
            android:textSize="16sp"
            android:textStyle="normal"
            android:maxLines="1"
            tools:text="Title" />

        <TextView
            android:id="@+id/playlist_item_tracks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="14dp"
            android:textColor="@color/gray_white_text"
            android:ellipsize="end"
            android:textSize="11sp"
            android:textStyle="normal"
            android:maxLines="1"
            tools:text="Description this text so long and I can see how it try to go second line" />
    </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>